global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "preflight"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["preflight:8000"]
        labels:
          service: "preflight-execution-firewall"
